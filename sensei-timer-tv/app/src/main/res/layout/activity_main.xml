<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0a0a0a">

    <!-- Tela de setup (primeira abertura) -->
    <LinearLayout
        android:id="@+id/setup_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="48dp"
        android:visibility="gone">

        <!-- Logo / Nome -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SENSEI TIMER"
            android:textColor="#e63030"
            android:textSize="42sp"
            android:textStyle="bold"
            android:letterSpacing="0.15"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Display para TV"
            android:textColor="#888888"
            android:textSize="16sp"
            android:layout_marginBottom="56dp" />

        <!-- Card de configuração -->
        <LinearLayout
            android:layout_width="480dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="32dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="CÓDIGO DA ACADEMIA"
                android:textColor="#aaaaaa"
                android:textSize="11sp"
                android:letterSpacing="0.12"
                android:layout_marginBottom="12dp" />

            <EditText
                android:id="@+id/uid_input"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:background="@drawable/input_bg"
                android:textColor="#ffffff"
                android:textSize="15sp"
                android:hint="Cole o código copiado do seu celular"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:inputType="textNoSuggestions"
                android:imeOptions="actionDone"
                android:maxLines="1"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tv_erro"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#e63030"
                android:textSize="12sp"
                android:layout_marginBottom="20dp"
                android:visibility="gone" />

            <Button
                android:id="@+id/btn_conectar"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:text="CONECTAR À ACADEMIA"
                android:textColor="#ffffff"
                android:textSize="14sp"
                android:textStyle="bold"
                android:letterSpacing="0.1"
                android:backgroundTint="#e63030"
                android:stateListAnimator="@null" />

        </LinearLayout>

        <TextView
            android:layout_width="480dp"
            android:layout_height="wrap_content"
            android:text="O código está disponível na aba TV do app no celular ou computador."
            android:textColor="#555555"
            android:textSize="13sp"
            android:textAlignment="center"
            android:layout_marginTop="20dp" />

    </LinearLayout>

    <!-- WebView (depois de configurado) -->
    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Botão flutuante redefinir (canto superior esquerdo, pequeno) -->
    <Button
        android:id="@+id/btn_redefinir"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="top|start"
        android:layout_margin="12dp"
        android:text="⚙"
        android:textColor="#ffffff"
        android:textSize="16sp"
        android:backgroundTint="#1a1a1a"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:stateListAnimator="@null"
        android:visibility="gone"
        android:alpha="0.5" />

</FrameLayout>
